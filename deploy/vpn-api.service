[Unit]
Description=VPN API Server (FastAPI)
After=network.target

[Service]
User=root
WorkingDirectory=/opt/vpn_service
ExecStart=/opt/vpn_service/.venv/bin/uvicorn main:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
